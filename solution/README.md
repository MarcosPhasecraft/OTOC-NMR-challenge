# solution/

The one directory anyone (you, a friend, an AI agent) is allowed to edit,
and only from Stage 2 onward — see PLAYBOOK.md Step 5. Contains one
function, `generate(spec) -> artifact`, plus optionally a `memory/`
subfolder of running notes on what's been tried.

Don't create this file's replacement until Stage 1 (PLAYBOOK.md Steps 1-4)
is actually signed off — it's meant to stay empty until then.
