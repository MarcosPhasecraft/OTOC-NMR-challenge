# registry.json and .score_cache.json are the project's record of what actually passed:
# commit them (the leaderboard is rendered from them). Scratch files from the
# write-then-rename in cache.py and registry.py only survive a crash between the write
# and the rename; the per-run attempt log and the dev/control caches are personal.

*.pyc
*.tmp
.DS_Store
.pytest_cache/
.venv/
__pycache__/
isolation/wheels/
results.jsonl
venv/
.dev_cache.json
.control_cache.json
.control_candidate.py
