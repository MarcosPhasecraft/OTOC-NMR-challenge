# harness/

This is where the frozen checker and scorer live, once you build them:
your `verify()`, your `score()`, the data-format helpers they both need,
and the `evaluate()` combinator. See PLAYBOOK.md Steps 3 and 5.

Also the home for `build_spec()`, `parse_instances()`, and `FROZEN_GLOBS`
— the small contract your problem exposes to the shared toolkit. See
`TOOLKIT.md`.

Frozen once Stage 1 (PLAYBOOK.md) is signed off — see the frozen/editable
rule in `CLAUDE.md`.

Nothing here yet. Delete this file once real content replaces it.
